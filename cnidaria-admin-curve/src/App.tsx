import React from 'react'

// This component is no longer used since we're using React Router
// The main.tsx file now uses RouterProvider directly
function App() {
  return (
    <div>
      <h1>App component - This should not be displayed</h1>
      <p>If you see this, there's a routing issue.</p>
    </div>
  )
}

export default App